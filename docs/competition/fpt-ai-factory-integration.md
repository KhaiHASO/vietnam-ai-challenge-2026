# FPT AI Factory evidence

The provider gateway selects FPT AI Factory when its API key and endpoint are configured. Its scorecard must report chat, embedding, rerank usage, p50/p95 latency, token/cost, cache savings, fallback rate, and degraded state. Gateway fallback is policy-controlled and must never bypass evidence/citation validation.
